{
  "name": "pi-herdr-outliner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "outliner": "bun run src/outliner.ts",
    "server": "bun run src/server-main.ts",
    "cli": "bun run src/cli.ts",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "typebox": "^1.0.44"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
